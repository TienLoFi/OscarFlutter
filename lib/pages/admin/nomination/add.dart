import 'package:flutter/material.dart';

class AdminNominationAdd extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Nomination - Add'),
      ),
    );
  }
}