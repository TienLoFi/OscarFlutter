import 'package:flutter/material.dart';

class AdminMovieDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Movie - Detail'),
      ),
    );
  }
}