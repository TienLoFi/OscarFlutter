import 'package:flutter/material.dart';

class AdminMovieEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Movie - Edit'),
      ),
    );
  }
}