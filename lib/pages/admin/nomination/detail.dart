import 'package:flutter/material.dart';

class AdminNominationDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Nomination - Detail'),
      ),
    );
  }
}