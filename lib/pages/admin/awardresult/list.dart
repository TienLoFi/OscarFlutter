import 'package:flutter/material.dart';

class AdminAwardResultList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Award Result - List'),
      ),
    );
  }
}