import 'package:flutter/material.dart';

class AdminAwardResultAdd extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Award Result - Add'),
      ),
    );
  }
}