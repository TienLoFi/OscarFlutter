import 'package:flutter/material.dart';

class AdminNominationEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Nomination - Edit'),
      ),
    );
  }
}