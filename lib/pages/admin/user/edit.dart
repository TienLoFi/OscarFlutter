import 'package:flutter/material.dart';

class AdminUserEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('User - Edit'),
      ),
    );
  }
}