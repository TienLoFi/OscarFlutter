import 'package:flutter/material.dart';

class SiteMainFeature extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Site - Main Add'),
      ),
    );
  }
}