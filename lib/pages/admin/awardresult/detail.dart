import 'package:flutter/material.dart';

class AdminAwardResultDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Award Result - Detail'),
      ),
    );
  }
}