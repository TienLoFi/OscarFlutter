import 'package:flutter/material.dart';

class AdminUserAdd extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('User - Add'),
      ),
    );
  }
}