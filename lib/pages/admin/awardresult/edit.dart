import 'package:flutter/material.dart';

class AdminAwardResultEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Award Result - Edit'),
      ),
    );
  }
}