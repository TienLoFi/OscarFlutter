import 'package:flutter/material.dart';

class AdminGroupEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Group - Edit'),
      ),
    );
  }
}