import 'package:flutter/material.dart';

class AdminGroupAdd extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Group - Add'),
      ),
    );
  }
}